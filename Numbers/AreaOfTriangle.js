//Area Of Triangle
// formula is 1/2 * base * height

function AreaOfTriangle(base,height){
    let Area = 0.5 * base * height;

    return Area;
}
triangleArea = AreaOfTriangle(4,4);
console.log(`Area of Triangle is: ${triangleArea}`)